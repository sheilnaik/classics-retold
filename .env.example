# OpenAI API Configuration
# Required for the translation preprocessing pipeline

# Get your API key from https://platform.openai.com/api-keys
OPENAI_API_KEY=your_api_key_here

# Optional: Override the default model (default: gpt-5-mini)
# OPENAI_MODEL=gpt-5-mini

# Note: Copy this file to .env and replace with your actual API key
# The .env file is gitignored and will not be committed to version control
